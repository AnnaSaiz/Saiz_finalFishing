using UnityEngine;

public class notesFish : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
//need
//if fish enters hook area, lock on and start biting 
//randomed timer for fish to get hooked
//water unity asset store
